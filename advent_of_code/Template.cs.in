using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;

namespace AOC
{
    using ChallengeType = string;

    [AdventOfCode(2020, 1)]
    public static class Template
    {
		[MapInput]
        public static IEnumerable<ChallengeType> Map(string[] lines)
        {
            return lines;
        }

        [Solver(1)]
        public static long Solve1(IEnumerable<ChallengeType> in)
        {
            return 1;
        }

        [Solver(2)]
        public static long Solve2(IEnumerable<ChallengeType> in)
        {
            return 1;
        }
    }
}
